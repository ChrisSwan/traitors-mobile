"""traitors_sim -- runnable alias package for the Mode B AI agent simulation.

The real implementation lives in ``traitors_mobile``; this package exists so
the contract's entry point ``python -m traitors_sim`` works.
"""

__version__ = "0.1.0"
